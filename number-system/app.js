const numbers = [
    19, 23, 4, 16, 28, 13, 31, 8, 29, 14, 6, 35, 2, 11, 17, 5, 9, 27, 12, 30,
  ];
let n = numbers.length;
for(let i = o;i<n;i++){
    if(n % i == 0 && n % n == 0 ){
        console.log("prime")
    }else{
        console.log("not")
    }
}  

const numbers2 = [
    19, 23, 4, 16, 28, 13, 31, 8, 29, 14, 6, 35, 2, 11, 17, 5, 9, 27, 12, 30,
  ];
  let max = INT_MIN;
  let min = INT_MAX;
   let sum = 0;
  let n2 = numbers.length;
  for(let i =0;i<n2;i++){
    sum += i;
  }
  console.log("sum is ",osum);

  for(let i=0;i<n2;i++){
    if(max > numbers2[i] )
        numbers2[i] = max;
  }

  console.log("max is ",max);
  for(let i=0;i<n2;i++){
    if(min < numbers2[i] )
        numbers2[i] = min;
  }

  console.log("min is ",min);

  
